{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Popup window: IPC to our own commands, plus revealing a cited source file in the file manager. Deliberately no filesystem, shell, or network capability — every path this app reads is chosen in Rust.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-reveal-item-in-dir"]
}
